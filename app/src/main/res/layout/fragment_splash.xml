<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    tools:background="@color/lightBlue"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/im_splash"
        android:layout_width="76dp"
        android:layout_height="77dp"
        android:layout_marginBottom="30dp"
        android:src="@drawable/ic_wallet_undefined"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_app_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="20sp"
        app:fontFamily="@font/cofo_sans_medium"
        app:layout_constraintTop_toBottomOf="@id/im_splash" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_made_by"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:textColor="@color/white"
        app:fontFamily="@font/mitr_light"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="by\nThe USS" />
</androidx.constraintlayout.widget.ConstraintLayout>